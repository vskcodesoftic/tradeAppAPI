import Bottleneck from 'bottleneck';
import { ExecuteResponse } from './sub-process';
interface PipEnvConfig {
    python?: string;
}
export declare const pipenvInstall: ((arg1: string, arg2: string[], arg3: PipEnvConfig) => Promise<ExecuteResponse>) & {
    withOptions: (options: Bottleneck.JobOptions, arg1: string, arg2: string[], arg3: PipEnvConfig) => Promise<ExecuteResponse>;
};
export {};
